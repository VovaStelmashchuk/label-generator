/** Name of the first-party cookie carrying the anonymous device id. */
export const DEVICE_COOKIE = 'lg_did';

/** One year - long enough to recognise a returning visitor, short enough to expire. */
export const DEVICE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
