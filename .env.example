# MongoDB connection string. A standalone server is enough - the app uses no
# transactions or change streams, so no replica set is required.
MONGO_URI="mongodb://root:example@localhost:27017/label-generator?authSource=admin"

# Optional. Defaults to the database encoded in MONGO_URI, then to "label-generator".
MONGO_DB="label-generator"
